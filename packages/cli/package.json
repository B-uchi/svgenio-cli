{
  "name": "@svgenio/cli",
  "version": "1.0.0",
  "description": "CLI tool for SVG to React component conversion",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "svgenio": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["svg", "react", "cli", "converter", "typescript"],
  "author": "Ibuchukwu Umeano",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/B-uchi/svgenio-cli.git",
    "directory": "packages/cli"
  },
  "bugs": {
    "url": "https://github.com/B-uchi/svgenio-cli/issues"
  },
  "homepage": "https://github.com/B-uchi/svgenio-cli#readme",
  "dependencies": {
    "@svgenio/core": "file:../core",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
